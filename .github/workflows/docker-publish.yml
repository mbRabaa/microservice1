name: CI/CD - Pipeline DevOps complet

on:
  push:
    branches: [main]

env:
  # Configuration Kubernetes
  K3S_VERSION: v1.26.5+k3s1
  NAMESPACE: microservice-1
  DOCKER_REGISTRY: docker.io
  
  # Configuration Application
  APP_NAME: gestion-trajet
  DEPLOYMENT_NAME: gestion-trajet-app
  SERVICE_NAME: gestion-trajet-service
  CONTAINER_PORT: 8080
  SERVICE_PORT: 80
  
  # Configuration Monitoring
  PROMETHEUS_PORT: 9090
  GRAFANA_PORT: 3000
  GRAFANA_ADMIN_PASSWORD: rabaa123
  
  # Configuration SonarQube
  SONAR_HOST_URL: http://localhost:9000
  
  # Configuration Email
  YOUR_EMAIL: elmbarkirabea@gmail.com
  EMAIL_FROM: "GitHub Actions <actions@github.com>"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout du code
        uses: actions/checkout@v3

      - name: 🔑 Connexion à Docker Hub 
        run: echo "${{ secrets.DOCKER_TOKEN }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: 🔨 Construire l'image Docker
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/gestion_trajet:latest .
          
      - name: 🚀 Pousser l'image sur Docker Hub
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/gestion_trajet:latest

  setup-and-test:
    name: "1. Installation et Tests"
    runs-on: ubuntu-latest
    steps:
      - name: "🛎️ Checkout du code"
        uses: actions/checkout@v4.1.1

      - name: "⎔ Configurer Node.js"
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'

      - name: "📦 Installer les dépendances"
        run: npm ci

      - name: "🧪 Tests Unitaires"
        run: npm run test:unit
        env:
          NODE_ENV: test

      - name: "🔗 Tests d'Intégration"
        run: npm run test:integration
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_USER: testuser
          DB_PASSWORD: testpass
          NODE_ENV: test

      - name: "🛠️ Build de l'application"
        run: npm run build

  sonarqube-analysis:
    name: "2. Analyse SonarQube"
    needs: setup-and-test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: "🛎️ Checkout du code"
        uses: actions/checkout@v4.1.1
        with:
          fetch-depth: 0

      - name: "🐳 Démarrer SonarQube"
        run: |
          docker run -d \
            --name sonarqube \
            -p 9000:9000 \
            -e SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true \
            sonarqube:9.9.1-community
          timeout 300 bash -c 'while ! curl -sSf ${{ env.SONAR_HOST_URL }}/api/system/status | grep -q "\"status\":\"UP\""; do
            sleep 10
            echo "En attente du démarrage..."
          done'

      - name: "🔑 Générer token SonarQube"
        run: |
          SONAR_TOKEN=$(curl -sS -u admin:admin \
            -X POST "${{ env.SONAR_HOST_URL }}/api/user_tokens/generate" \
            -d "name=github-action-$(date +%s)" \
            | jq -r '.token')
          echo "SONAR_TOKEN=$SONAR_TOKEN" >> $GITHUB_ENV
          echo "::add-mask::$SONAR_TOKEN"

      - name: "🔍 Exécuter analyse SonarQube"
        uses: SonarSource/sonarqube-scan-action@v4
        env:
          SONAR_TOKEN: ${{ env.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ env.SONAR_HOST_URL }}
        with:
          args: >
            -Dsonar.projectKey=gestion-trajet-app
            -Dsonar.sources=src
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info

      - name: "🧹 Nettoyer SonarQube"
        if: always()
        run: docker rm -f sonarqube || true

  kubernetes-deploy:
    name: "3. Déploiement Kubernetes"
    needs: sonarqube-analysis
    runs-on: ubuntu-latest
    steps:
      - name: "🛎️ Checkout du code"
        uses: actions/checkout@v4.1.1

      - name: "🔐 Login Docker Hub"
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: "🖥️ Installer K3s"
        run: |
          curl -sfL https://get.k3s.io | INSTALL_K3S_VERSION=${{ env.K3S_VERSION }} sh -
          sudo chmod 644 /etc/rancher/k3s/k3s.yaml

      - name: "⚙️ Configurer kubectl"
        run: |
          mkdir -p $HOME/.kube
          sudo cp /etc/rancher/k3s/k3s.yaml $HOME/.kube/config
          sudo chown $(id -u):$(id -g) $HOME/.kube/config

      - name: "🧹 Nettoyer avant déploiement"
        run: |
          kubectl delete deployment ${{ env.DEPLOYMENT_NAME }} -n ${{ env.NAMESPACE }} --ignore-not-found=true || true
          kubectl delete service ${{ env.SERVICE_NAME }} -n ${{ env.NAMESPACE }} --ignore-not-found=true || true
          kubectl delete configmap app-config -n ${{ env.NAMESPACE }} --ignore-not-found=true || true
          kubectl delete secret database-credentials -n ${{ env.NAMESPACE }} --ignore-not-found=true || true

      - name: "🚀 Déployer l'infrastructure"
        run: |
          kubectl create ns ${{ env.NAMESPACE }} --dry-run=client -o yaml | kubectl apply -f -
          kubectl apply -f k8s/infra/coredns -n kube-system || true
          kubectl apply -f k8s/app/db -n ${{ env.NAMESPACE }} || true

      - name: "🚀 Déployer l'application"
        run: |
          kubectl apply -f k8s/app -n ${{ env.NAMESPACE }} || true
          kubectl rollout status deployment/${{ env.DEPLOYMENT_NAME }} -n ${{ env.NAMESPACE }} --timeout=300s || true

      - name: "🔍 Vérifier le déploiement"
        run: |
          # Simple pod status check
          echo "=== État des Pods ==="
          kubectl get pods -n ${{ env.NAMESPACE }} --no-headers | grep -v "Running" && exit 1 || echo "Tous les pods sont en état Running"
          
          # Basic health check
          kubectl port-forward svc/${{ env.SERVICE_NAME }} 8080:${{ env.CONTAINER_PORT }} -n ${{ env.NAMESPACE }} >/dev/null 2>&1 &
          sleep 5
          if curl -sSf http://localhost:8080/health; then
            echo "✅ Application accessible"
          else
            echo "❌ Échec de connexion"
            exit 1
          fi

      - name: "🔄 Rollback en cas d'échec"
        if: failure()
        run: |
          kubectl rollout undo deployment/${{ env.DEPLOYMENT_NAME }} -n ${{ env.NAMESPACE }} || true
          exit 1

      - name: "📧 Notification d'échec"
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          from: ${{ env.EMAIL_FROM }}
          to: ${{ env.YOUR_EMAIL }}
          subject: "Échec de déploiement - ${{ env.APP_NAME }}"
          body: |
            Le déploiement a échoué.
            Commit: ${{ github.sha }}
            Logs: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

  # 5ème job: Déploiement du monitoring
  monitoring-deploy:
    name: "4. Déploiement Monitoring"
    needs: kubernetes-deploy
    runs-on: ubuntu-latest
    env:
      PROMETHEUS_PORT: 9090
      GRAFANA_PORT: 3000
      NODEJS_PORT: 8081
      
    steps:
      - name: "🛎️ Checkout du code"
        uses: actions/checkout@v4

      - name: "🐳 Installer Docker Compose"
        run: |
          sudo curl -L "https://github.com/docker/compose/releases/download/v2.23.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          sudo chmod +x /usr/local/bin/docker-compose

      - name: "🧹 Nettoyer l'environnement"
        working-directory: ./monitoring
        run: docker-compose down -v --remove-orphans || true

      - name: "🚀 Démarrer le monitoring"
        working-directory: ./monitoring
        run: |
          docker-compose up -d --build
          echo "Attente du démarrage des services (30s)..."
          sleep 30

      - name: "🔍 Vérifier les services"
        working-directory: ./monitoring
        run: |
          if ! docker-compose ps | grep -q "Up"; then
            echo "❌ Certains services ne sont pas démarrés"
            docker-compose logs
            exit 1
          fi
          
          SERVICES=("nodejs-app:8080" "prometheus:9090" "grafana:3000")
          for service in "${SERVICES[@]}"; do
            if ! docker-compose ps | grep "$service" | grep -q "Up"; then
              echo "❌ Service $service non démarré"
              exit 1
            fi
          done
          echo "✅ Tous les services sont opérationnels"

      - name: "📊 Configurer Grafana"
        working-directory: ./monitoring
        env:
          GRAFANA_ADMIN_PASSWORD: ${{ secrets.GRAFANA_ADMIN_PASSWORD }}
        run: |
          until curl -s http://localhost:$GRAFANA_PORT/api/health; do
            echo "En attente de Grafana..."
            sleep 5
          done
          
          curl -X POST "http://localhost:$GRAFANA_PORT/api/datasources" \
            -H "Content-Type: application/json" \
            -u "admin:$GRAFANA_ADMIN_PASSWORD" \
            -d '{
              "name":"Prometheus",
              "type":"prometheus",
              "url":"http://prometheus:9090",
              "access":"proxy",
              "basicAuth":false,
              "isDefault":true
            }'
          
          echo "Configuration Grafana terminée"

      - name: "🧪 Tests de fonctionnement"
        run: |
          if ! curl -s http://localhost:$PROMETHEUS_PORT/-/ready | grep -q "Prometheus"; then
            echo "❌ Prometheus non prêt"
            exit 1
          fi
          
          if ! curl -s http://localhost:$NODEJS_PORT/metrics | grep -q "process_cpu_seconds_total"; then
            echo "❌ Métriques Node.js non disponibles"
            exit 1
          fi
          
          if ! curl -s http://localhost:$GRAFANA_PORT/api/health | grep -q "\"database\":\"ok\""; then
            echo "❌ Grafana non prêt"
            exit 1
          fi

      - name: "📝 Générer un rapport"
        working-directory: ./monitoring
        run: |
          echo "# Rapport de Monitoring" > monitoring-report.md
          echo "## État des Services" >> monitoring-report.md
          docker-compose ps >> monitoring-report.md
          
          echo "## Cibles Prometheus" >> monitoring-report.md
          curl -s http://localhost:$PROMETHEUS_PORT/api/v1/targets | jq . >> monitoring-report.md
          
          echo "## Métriques Node.js" >> monitoring-report.md
          curl -s http://localhost:$NODEJS_PORT/metrics | head -n 20 >> monitoring-report.md
          
          echo "Rapport généré :"
          cat monitoring-report.md