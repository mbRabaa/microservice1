name: CI/CD - Pipeline DevOps & K3s Deployment

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build and analyze
    runs-on: ubuntu-latest
    services:
      sonarqube:
        image: sonarqube:lts-community
        ports:
          - 9000:9000
        env:
          SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
          SONAR_WEB_JAVAOPTS: "-Xmx512m -Xms128m"
        options: >-
          --health-cmd "curl -f http://localhost:9000/api/server/version || exit 1"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 60
          --memory 2g
          --cpus 2
    
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Afficher les logs si le service échoue
      - name: 📜 Afficher les logs SonarQube (si échec)
        if: failure()
        run: |
          echo "=== Logs du conteneur SonarQube ==="
          docker ps -a
          docker logs $(docker ps -aq --filter ancestor=sonarqube:lts-community) || true
          echo "==================================="

      # Étape 1 : Installer les dépendances
      - name: 📦 Installer les dépendances
        run: npm install

      # Étape 2 : Lancer les tests unitaires
      - name: 🎯 Lancer les tests unitaires
        run: npm test

      # Étape 3 : Attendre que SonarQube soit prêt
      - name: ⏳ Attendre que SonarQube soit prêt (10 min max)
        timeout-minutes: 10
        run: |
          echo "Vérification du démarrage de SonarQube..."
          for i in {1..60}; do
            if curl -sSf http://localhost:9000 >/dev/null; then
              echo "SonarQube est prêt après $((i*10)) secondes"
              exit 0
            fi
            echo "Attente ($((i*10))s)..."
            sleep 10
          done
          echo "Erreur: SonarQube n'a pas démarré après 10 minutes"
          exit 1

      # Étape 4 : Analyse SonarQube
      - name: 🔍 Analyser la qualité du code
        uses: SonarSource/sonarqube-scan-action@v4
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: http://localhost:9000

      # Étape 5 : Quality Gate
      - name: 🚦 Vérifier la Quality Gate
        uses: SonarSource/sonarqube-quality-gate-action@v1
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  # [Reste de votre configuration existante...]